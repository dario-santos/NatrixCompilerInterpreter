mov %rax, %esi ; variável do loop
mov %rax, %dsi

l:            ; LABEL
  inc %esi    ; incrementar
  cmp %esi %dsi
  jnz l       ; enquanto não for chegarmos a zero
.# Código


mov $1, %esi
l:
  write $hello, $len
  inc %esi
  cmp %dsi, %esi
  jle l


for i in 5 .. 10
{
    print(i);
}