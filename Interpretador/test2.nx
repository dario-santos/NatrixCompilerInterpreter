type small_int = [0 .. 10];
   val pipo : int = 4;
function int_to_small_int(x) : int
{
    val pipo : int = 3;

    foreach i in 0 .. 10
    {   val pipo : int = 2;
        if(x == i)
        {
            print(pipo);

            val tmp : small_int = x;
            return tmp;
        }

    }

    return 0;
}

print(int_to_small_int(10));