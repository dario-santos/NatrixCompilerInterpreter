type bool = [0 .. 1];
type stack = [0 .. 10];

val myStack : stack = 0;
val is_full : bool = 0;

foreach i in 0 .. 100
{
    // Aqui vai dar erro no Sassign
    myStack := myStack + 1;
}

if(is_full)
{
    print(100);
}
else 
{
    print(0);
}
